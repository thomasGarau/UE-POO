question 3.1

1) la classe java.lang.math posséde 2attributs
2) elles sont définit en static ce sont des attributs et méthode de classe
3)random()
4) 4 :
max(double a, double b)
max(float a, float b)
max(int a, int b)
max(long a, long b)
la différence entre ces différente signature est le type des attributs en parametre.

5)
a) int x= Math.max(5)
b) int x= Math.max(5,6)

a va produire une erreur car le nombre d'argument ne correspond pas.
b va return 6 

question 3.2

1)
2) ca ne peut pas marcher car il n'y a qu'un seule string or la fonction en attend deux 
str.length